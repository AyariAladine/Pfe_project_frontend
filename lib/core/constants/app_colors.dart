import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - Dark Navy Blue
  static const Color primary = Color(0xFF1E3A5F);        // Dark navy blue
  static const Color primaryLight = Color(0xFF2E5077);   // Lighter navy
  static const Color primaryDark = Color(0xFF152A45);    // Darker navy
  
  // Secondary gradient color
  static const Color primaryAccent = Color(0xFF3B82F6);  // Bright blue accent

  // Secondary Colors - Teal
  static const Color secondary = Color(0xFF14B8A6);      // Teal
  static const Color secondaryLight = Color(0xFF2DD4BF);
  static const Color secondaryDark = Color(0xFF0D9488);

  // Accent Colors - Coral/Orange for CTAs
  static const Color accent = Color(0xFFF97316);         // Orange
  static const Color accentLight = Color(0xFFFB923C);

  // Background Colors - Light Mode (Clean with subtle warmth)
  static const Color background = Color(0xFFFAFAFB);     // Almost white
  static const Color surface = Color(0xFFFFFFFF);        // Pure white
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Background Colors - Dark Mode (kept for future if needed)
  static const Color backgroundDark = Color(0xFF0F172A);
  static const Color surfaceDark = Color(0xFF1E293B);
  static const Color cardBackgroundDark = Color(0xFF334155);

  // Text Colors - Light
  static const Color textPrimary = Color(0xFF1E1B4B);    // Deep indigo-black
  static const Color textSecondary = Color(0xFF6B7280);  // Cool grey
  static const Color textHint = Color(0xFF9CA3AF);       // Light grey
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Text Colors - Dark (for dark mode)
  static const Color textPrimaryDark = Color(0xFFF1F5F9);
  static const Color textSecondaryDark = Color(0xFF94A3B8);
  static const Color textHintDark = Color(0xFF64748B);

  // Status Colors
  static const Color success = Color(0xFF10B981);        // Emerald
  static const Color error = Color(0xFFEF4444);          // Red
  static const Color warning = Color(0xFFF59E0B);        // Amber
  static const Color info = Color(0xFF3B82F6);           // Blue

  // Border Colors
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFE5E7EB);
  static const Color borderDark = Color(0xFF475569);
  static const Color dividerDark = Color(0xFF475569);

  // Gradient Colors
  static const Color gradientStart = Color(0xFF4F46E5);  // Indigo
  static const Color gradientMiddle = Color(0xFF7C3AED); // Purple
  static const Color gradientEnd = Color(0xFF06B6D4);    // Cyan

  // Social Colors
  static const Color google = Color(0xFFDB4437);
  static const Color facebook = Color(0xFF1877F2);
  static const Color apple = Color(0xFF000000);
}
